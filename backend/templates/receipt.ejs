<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Receipt - <%= receiptNumber %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: #333;
            background: white;
            padding: 20px;
        }

        .receipt-container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            padding: 40px;
            border: 1px solid #ddd;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #007bff;
            padding-bottom: 20px;
        }

        .logo {
            width: 80px;
            height: 80px;
            margin: 0 auto 15px;
            border-radius: 50%;
            background: #007bff;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 24px;
            font-weight: bold;
        }

        .company-name {
            font-size: 28px;
            font-weight: bold;
            color: #007bff;
            margin-bottom: 5px;
        }

        .company-details {
            font-size: 14px;
            color: #666;
            line-height: 1.4;
        }

        .receipt-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 8px;
        }

        .receipt-info-left,
        .receipt-info-right {
            flex: 1;
        }

        .receipt-info-right {
            text-align: right;
        }

        .receipt-number {
            font-size: 18px;
            font-weight: bold;
            color: #007bff;
            margin-bottom: 10px;
        }

        .info-label {
            font-weight: bold;
            color: #333;
            margin-bottom: 5px;
        }

        .info-value {
            color: #666;
            margin-bottom: 10px;
        }

        .customer-info {
            margin-bottom: 30px;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 8px;
        }

        .customer-info h3 {
            color: #007bff;
            margin-bottom: 15px;
            font-size: 18px;
        }

        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
        }

        .items-table th,
        .items-table td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        .items-table th {
            background: #007bff;
            color: white;
            font-weight: bold;
        }

        .items-table tbody tr:nth-child(even) {
            background: #f8f9fa;
        }

        .items-table .text-right {
            text-align: right;
        }

        .items-table .text-center {
            text-align: center;
        }

        .totals {
            float: right;
            width: 300px;
            margin-bottom: 30px;
        }

        .totals-row {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            border-bottom: 1px solid #eee;
        }

        .totals-row.subtotal {
            font-size: 16px;
        }

        .totals-row.tax {
            font-size: 16px;
            color: #666;
        }

        .totals-row.total {
            font-size: 20px;
            font-weight: bold;
            color: #007bff;
            border-bottom: 2px solid #007bff;
            border-top: 2px solid #007bff;
            margin-top: 10px;
            padding-top: 15px;
            padding-bottom: 15px;
        }

        .payment-info {
            clear: both;
            margin-bottom: 30px;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 8px;
        }

        .payment-info h3 {
            color: #007bff;
            margin-bottom: 15px;
        }

        .payment-method {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }

        .footer {
            text-align: center;
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            color: #666;
            font-size: 14px;
        }

        .tax-info {
            margin-top: 20px;
            padding: 15px;
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 6px;
            font-size: 12px;
        }

        .tax-info strong {
            color: #856404;
        }

        .status-badge {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: bold;
            text-transform: uppercase;
        }

        .status-completed {
            background: #d4edda;
            color: #155724;
        }

        .status-pending {
            background: #fff3cd;
            color: #856404;
        }

        .status-cancelled {
            background: #f8d7da;
            color: #721c24;
        }

        @media print {
            body {
                padding: 0;
            }
            
            .receipt-container {
                border: none;
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="receipt-container">
        <!-- Header -->
        <div class="header">
            <div class="logo">K</div>
            <div class="company-name"><%= companyName %></div>
            <div class="company-details">
                <%= companyAddress %><br>
                Phone: <%= companyPhone %> | Email: <%= companyEmail %><br>
                Website: <%= companyWebsite %>
            </div>
        </div>

        <!-- Receipt Info -->
        <div class="receipt-info">
            <div class="receipt-info-left">
                <div class="receipt-number">Receipt #<%= receiptNumber %></div>
                <div class="info-label">Date:</div>
                <div class="info-value"><%= date %> at <%= time %></div>
                <div class="info-label">Order ID:</div>
                <div class="info-value">#<%= id %></div>
            </div>
            <div class="receipt-info-right">
                <div class="info-label">Status:</div>
                <div class="info-value">
                    <span class="status-badge status-<%= status.toLowerCase() %>"><%= status %></span>
                </div>
                <% if (pickupDate) { %>
                <div class="info-label">Pickup Date:</div>
                <div class="info-value"><%= formatDate(pickupDate) %></div>
                <% } %>
                <% if (deliveryDate) { %>
                <div class="info-label">Delivery Date:</div>
                <div class="info-value"><%= formatDate(deliveryDate) %></div>
                <% } %>
            </div>
        </div>

        <!-- Customer Info -->
        <div class="customer-info">
            <h3>Customer Information</h3>
            <div class="info-label">Name:</div>
            <div class="info-value"><%= customerName || 'N/A' %></div>
            
            <% if (customerPhone) { %>
            <div class="info-label">Phone:</div>
            <div class="info-value"><%= customerPhone %></div>
            <% } %>
            
            <% if (customerEmail) { %>
            <div class="info-label">Email:</div>
            <div class="info-value"><%= customerEmail %></div>
            <% } %>
            
            <% if (pickupAddress) { %>
            <div class="info-label">Pickup Address:</div>
            <div class="info-value"><%= pickupAddress %></div>
            <% } %>
            
            <% if (deliveryAddress && deliveryAddress !== pickupAddress) { %>
            <div class="info-label">Delivery Address:</div>
            <div class="info-value"><%= deliveryAddress %></div>
            <% } %>
        </div>

        <!-- Items Table -->
        <table class="items-table">
            <thead>
                <tr>
                    <th>Service</th>
                    <th class="text-center">Quantity</th>
                    <th class="text-right">Unit Price</th>
                    <th class="text-right">Total</th>
                </tr>
            </thead>
            <tbody>
                <% items.forEach(item => { %>
                <tr>
                    <td>
                        <strong><%= item.name %></strong>
                        <% if (item.description) { %>
                        <br><small style="color: #666;"><%= item.description %></small>
                        <% } %>
                        <% if (item.specialInstructions) { %>
                        <br><small style="color: #007bff; font-style: italic;">Note: <%= item.specialInstructions %></small>
                        <% } %>
                    </td>
                    <td class="text-center"><%= item.quantity %></td>
                    <td class="text-right"><%= formatCurrency(item.price) %></td>
                    <td class="text-right"><%= formatCurrency(item.price * item.quantity) %></td>
                </tr>
                <% }); %>
            </tbody>
        </table>

        <!-- Totals -->
        <div class="totals">
            <div class="totals-row subtotal">
                <span>Subtotal:</span>
                <span><%= subtotal %></span>
            </div>
            
            <% if (discount && discount > 0) { %>
            <div class="totals-row">
                <span>Discount:</span>
                <span>-<%= formatCurrency(discount) %></span>
            </div>
            <% } %>
            
            <div class="totals-row tax">
                <span>Tax (<%= taxRate %>%):</span>
                <span><%= taxAmount %></span>
            </div>
            
            <% if (tip && tip > 0) { %>
            <div class="totals-row">
                <span>Tip:</span>
                <span><%= formatCurrency(tip) %></span>
            </div>
            <% } %>
            
            <div class="totals-row total">
                <span>Total:</span>
                <span><%= total %></span>
            </div>
        </div>

        <!-- Payment Info -->
        <div class="payment-info">
            <h3>Payment Information</h3>
            <div class="payment-method">
                <span><strong>Payment Method:</strong></span>
                <span><%= paymentMethod || 'Card' %></span>
            </div>
            <% if (paymentStatus) { %>
            <div class="payment-method">
                <span><strong>Payment Status:</strong></span>
                <span class="status-badge status-<%= paymentStatus.toLowerCase() %>"><%= paymentStatus %></span>
            </div>
            <% } %>
            <% if (transactionId) { %>
            <div class="payment-method">
                <span><strong>Transaction ID:</strong></span>
                <span><%= transactionId %></span>
            </div>
            <% } %>
        </div>

        <!-- Tax Information -->
        <% if (taxNumber || gstNumber) { %>
        <div class="tax-info">
            <% if (taxNumber) { %>
            <strong>Tax ID:</strong> <%= taxNumber %><br>
            <% } %>
            <% if (gstNumber) { %>
            <strong>GST Number:</strong> <%= gstNumber %><br>
            <% } %>
            This receipt serves as proof of payment and can be used for tax purposes.
        </div>
        <% } %>

        <!-- Footer -->
        <div class="footer">
            <p><strong>Thank you for choosing <%= companyName %>!</strong></p>
            <p>For questions about this receipt, please contact us at <%= companyEmail %> or <%= companyPhone %></p>
            <p>This is a computer-generated receipt and does not require a signature.</p>
            <p style="margin-top: 10px; font-size: 12px; color: #999;">
                Generated on <%= formatDate(new Date()) %> at <%= formatTime(new Date()) %>
            </p>
        </div>
    </div>
</body>
</html>
